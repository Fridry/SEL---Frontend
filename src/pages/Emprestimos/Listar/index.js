import React from "react";

import Template from "../../../components/Template";

const Listar = () => {
  return (
    <Template title="Listar empréstimos">
      <h1>Listar empréstimos</h1>
    </Template>
  );
};

export default Listar;
